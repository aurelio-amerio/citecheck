#!/bin/bash
if ! _SELF="$(readlink -f "$0" 2>/dev/null)"; then
    _SELF="$(python3 -c 'import os,sys; print(os.path.realpath(sys.argv[1]))' "$0")"
fi
PLUGIN_ROOT="$(dirname "$(dirname "$_SELF")")"
exec python3 "${PLUGIN_ROOT}/skills/citecheck-deep/scripts/collate_deep_report.py" "$@"
